Ans : 01

var: Only works inside the function where it is declared and it can be changed.

Let: It is block scoped . the value can be changed but cannot be redeclared in the same block


const: It is also block. scoped the value cannot be changed after declaration




Ans : 02

Used to expand or copy elements from an array or object

ex: let a = [1,2]
let b = [...a,3]
 output : [1,2,3]




Ans :03 


Map() creates a new array after modifying each element
Filter() creates a new array with selected elements
ForEach() loop through array but does not return a new array






Ans : 04
It is a short way to write a function in Javascript
const add = (a,b) => a+b;



Ans : 05

Used to write strings with variables and it uses ` `
ex: let name = "Rifat"
console.log (`Hello ${name}`);